# Application
APP_NAME="Baby Names Social Network"
APP_VERSION="0.1.0"
DEBUG=true
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8000
WORKERS=1

# Database - Names
NAMES_DB_USER=postgres
NAMES_DB_PASSWORD=postgres
NAMES_DB_HOST=localhost
NAMES_DB_PORT=5432
NAMES_DB_NAME=babynames_db

# Database - Users
USERS_DB_USER=postgres
USERS_DB_PASSWORD=postgres
USERS_DB_HOST=localhost
USERS_DB_PORT=5433
USERS_DB_NAME=users_db

# Redis - Cache
REDIS_CACHE_HOST=localhost
REDIS_CACHE_PORT=6379
REDIS_CACHE_DB=0

# Redis - Sessions
REDIS_SESSIONS_HOST=localhost
REDIS_SESSIONS_PORT=6380
REDIS_SESSIONS_DB=0

# Authentication
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32-to-generate
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=30

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Email (optional, for user verification)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=
EMAILS_FROM_NAME=

# External APIs (optional)
OPENAI_API_KEY=
